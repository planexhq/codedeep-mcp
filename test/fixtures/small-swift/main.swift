// Application entry point — top-level executable statements.
let g = Greeter(prefix: "Hi")
let msg = g.greet("world")
let _ = normalize(msg)
let c = defaultCircle()
let _ = c.area()
let _ = c.label()
