node_modules/
dist/
# Root-anchored: the regenerated grammars/ dir at the repo root is ignored, but
# vendor/grammars/ (committed source wasms copied from there) must stay tracked.
/grammars/
.codedeep/
*.log
.DS_Store

# Dogfooding harness (scripts/dogfood): cloned repos + run outputs
dogfood-cache/
dogfood-runs/
